<li>
  <h3><%= question.body %></h3>
  <%= link_to t('shared.show'), admin_question_path(question) %> |
  <%= link_to t('shared.edit'), edit_admin_question_path(question) %> |
  <%= link_to t('shared.delete'),
              admin_question_path(question),
              method: :delete,
              data: { confirm: t('shared.confirm') } %>
</li>
